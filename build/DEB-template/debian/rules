#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_build:
	echo "nothing to compile"

override_dh_fixperms:
	dh_fixperms
	chmod 755 debian/easyplayer/opt/easyplayer/EasyPlayer.py

override_dh_builddeb:
	dh_builddeb -- -Zgzip
